export type LoginRequestDto = {
  email: string;
  password: string;
};

export type RegisterRequestDto = {
  name: string;
  surname: string;
  email: string;
  phoneNumber: string;
  password: string;
};
